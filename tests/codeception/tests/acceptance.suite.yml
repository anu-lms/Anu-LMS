class_name: AcceptanceTester
modules:
    enabled:
        - Asserts
        - WebDriver
        - \Helper\Acceptance
    config:
      WebDriver:
        port: 4444
        wait: 0.5
        browser: chrome
        restart: true
        window_size: 1280x960
        connection_timeout: 60
        request_timeout: 60
        capabilities:
            unexpectedAlertBehaviour: 'accept'
